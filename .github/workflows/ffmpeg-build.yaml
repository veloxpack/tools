name: Build FFMPEG image

on:
  push:
    branches:
      - main
    paths:
      - 'ffmpeg/Dockerfile'
      - '.github/workflows/ffmpeg-build.yaml'

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository_owner }}/ffmpeg
  ALPINE_VERSION: "3.22.2"
  LAME_VERSION: "3.100"
  OPUS_VERSION: v1.4
  LIBVPX_VERSION: "v1.13.0"
  X264_VERSION: "0480cb05fa188d37ae87e8f4fd8f1aea3711f7ee"
  X265_VERSION: "3.5"
  MBEDTLS_VERSION: "v3.4.1"
  FFMPEG_VERSION: "8.0"

jobs:
  build-and-push-image:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
      attestations: write
      id-token: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push Docker image
        id: push
        uses: docker/build-push-action@v6
        with:
          context: ./ffmpeg
          file: ./ffmpeg/Dockerfile
          platforms: linux/amd64,linux/arm64
          push: true
          tags: |
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ env.FFMPEG_VERSION }}
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest
          labels: |
            org.opencontainers.image.title=FFMPEG
            org.opencontainers.image.description=FFMPEG Docker image optimized for video transcoding with modern codecs
            org.opencontainers.image.vendor=VeloxPack
            org.opencontainers.image.version=${{ env.FFMPEG_VERSION }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
          provenance: false
          sbom: false
          build-args: |
            ALPINE_VERSION=${{ env.ALPINE_VERSION }}
            LAME_VERSION=${{ env.LAME_VERSION }}
            OPUS_VERSION=${{ env.OPUS_VERSION }}
            LIBVPX_VERSION=${{ env.LIBVPX_VERSION }}
            X264_VERSION=${{ env.X264_VERSION }}
            X265_VERSION=${{ env.X265_VERSION }}
            MBEDTLS_VERSION=${{ env.MBEDTLS_VERSION }}
            FFMPEG_VERSION=${{ env.FFMPEG_VERSION }}

      - name: Generate artifact attestation
        continue-on-error: true
        uses: actions/attest-build-provenance@v3
        with:
          subject-name: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          subject-digest: ${{ steps.push.outputs.digest }}
          push-to-registry: true

      - name: Summary
        run: |
          cat >> "${GITHUB_STEP_SUMMARY}" <<EOF
          ## 🐳 Docker Image Published to GHCR

          **Image:** \`${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}\`
          **Version:** \`${{ env.FFMPEG_VERSION }}\`
          **Digest:** \`${{ steps.push.outputs.digest }}\`

          ### Pull Image

          \`\`\`bash
          docker pull ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest
          \`\`\`

          ### Specific Version

          \`\`\`bash
          docker pull ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ env.FFMPEG_VERSION }}
          \`\`\`

          ### Verify Attestation

          \`\`\`bash
          gh attestation verify \\
            oci://${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest \\
            --owner ${{ github.repository_owner }}
          \`\`\`

          ### Make Package Public

          📝 **Important:** By default, packages are private. To make this image publicly accessible:

          1. Go to: https://github.com/${{ github.repository_owner }}/packages
          2. Click on the \`ffmpeg\` package
          3. Click "Package settings"
          4. Scroll to "Danger Zone"
          5. Click "Change visibility" → Select "Public"
          EOF

  test:
    needs: build-and-push-image
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: read

    steps:
      - name: Clone the code
        uses: actions/checkout@v5

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version-file: go.mod

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Pull Docker image
        run: docker pull ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest

      - name: Setup test environment
        run: make test-setup

      - name: Run tests
        run: make test-ffmpeg

